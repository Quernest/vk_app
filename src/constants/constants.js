const POSTER = require('../../assets/images/poster.png');

// requests
const FRIENDS = 'friends';
const USERS = 'users';
const STATUS = 'status';
const NEWSFEED = 'newsfeed';

export {
  POSTER,
  FRIENDS,
  USERS,
  STATUS,
  NEWSFEED
};
